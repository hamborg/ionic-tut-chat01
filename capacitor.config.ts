import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ion-tut-chat01',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
